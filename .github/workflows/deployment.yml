name: Build and Publish Docker Image

on:
  push:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up QUEMU
        uses: docker/setup-quemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to the Container registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Build and push
        uses: docker/build-pusg-action@v4
        witch:
          context: ./app
          file: .app/Dockerfile
          push: true
          tags: ghcr.io/${{github.repository_owner}}/net-sentinel:latest
          build-args: |
            GO_VERSION=1.21.5
          plataforms: linux/amd64, linux/arm64
          

     